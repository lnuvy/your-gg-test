export type tierHistory = {
  division: string
  leaguePoint: number
  loses: number
  normalizedPoint: number
  tier: string
  updated: number
  wins: number
}

export type tierHistoryArr = {
  tierHistoryArr: tierHistory[]
}
